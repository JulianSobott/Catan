import java.io.Serializable;
import java.util.Scanner;



public class Debug {
	
	/*public static void main(String[] args) {
		LocalDataServer server = new LocalDataServer();
		RemoteDataClient client = new RemoteDataClient(null, "192.168.2.118");
		server.messageTo(0, new Packet(Command.DICE, new Packet.DiceResult((byte) 4)));
		Packet p = new Packet("Some Code");
		client.sendMessage(p);
		server.messageTo(0, new Packet("Message From Server to Client"));
		new Packet.DiceResult((byte) 0);
	}*/
}
